
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: grafana-operator
  namespace: user1-flying-djl
  labels:
    operators.coreos.com/grafana-operator.user1-flying-djl: ""
spec:
  name: grafana-operator

  # OCP 4.12 / 4.13
  #   oc get packagemanifests grafana-operator -n openshift-marketplace -o json | jq -r .status.channels[].name
  channel: v5

  installPlanApproval: Automatic
  source: community-operators
  sourceNamespace: openshift-marketplace
